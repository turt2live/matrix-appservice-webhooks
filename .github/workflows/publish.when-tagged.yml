name: ci

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: 🔍 Checkout
        uses: actions/checkout@v2

      - name: ⚙ Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: 🔑 Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: 🔑 Login to GitHub Container Registry
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 🚀 Build and push
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          tags: |
            netresearch/matrix-appservice-webhooks:latest
            netresearch/matrix-appservice-webhooks:${{ github.ref }}
            ghcr.io/netresearch/matrix-appservice-webhooks:latest
            ghcr.io/netresearch/matrix-appservice-webhooks:${{ github.ref }}
